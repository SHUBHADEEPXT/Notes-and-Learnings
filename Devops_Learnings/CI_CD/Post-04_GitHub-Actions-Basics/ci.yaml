
---

### 📄 `ci.yml` file

```yaml
name: Simple CI
on: [push]
jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v3
      - name: Install & Test
        run: |
          npm install
          npm test
